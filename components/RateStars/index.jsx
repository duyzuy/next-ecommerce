import * as Icon from 'semantic-ui-react';
import { makeArrayFromLength } from '../../utils/helper';
const RateStars = ({ stars = 5, rating }) => {
  const arrStar = makeArrayFromLength(stars);
};
export default RateStars;
